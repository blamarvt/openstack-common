#!/bin/bash
TOOLS="$( cd "$( dirname "$0" )" && pwd )"
CLEAN=0

for arg in $*
do
    case $arg in
        --clean) CLEAN=1;;
    esac
done

if [[ $CLEAN -eq 1 ]]; then
	cd $TOOLS/..
    tox -r -c $TOOLS/tox.ini -e ALL
else
	cd $TOOLS/..
	tox -c $TOOLS/tox.ini -e ALL
fi
